Objective: Develop a comprehensive research-backed strategy to migrate the unit test suite from JUnit 4 to JUnit 5. The primary goal is to enable the use of JUnit 5's @ParameterizedTest for more robust testing of
  the ResonanceEngine.

  Your plan must address the following:


   1. Infrastructure & Dependencies: Identify the necessary updates for libs.versions.toml and app/build.gradle.kts. This includes adding the JUnit 5 Jupiter engine and evaluating the need for the android-junit5
      Gradle plugin.
   2. Lifecycle & Annotation Migration: Detail the mapping of JUnit 4 annotations (e.g., @Test, @Before, @After, @Ignore) to their JUnit 5 equivalents.
   3. Handling Rules & Extensions: Analyze existing @Rule usage (e.g., InstantTaskExecutorRule or Hilt's HiltAndroidRule) and provide a strategy for migrating them to JUnit 5 @ExtendWith or equivalent extensions.
      Highlight any known compatibility issues with Hilt and JUnit 5 in an Android environment.
   4. ResonanceEngine Expansion: Design a specific step to rewrite ResonanceEngineTest.kt using @ParameterizedTest. Outline the input/output sets (e.g., @ValueSource, @MethodSource) needed to cover edge cases, mood
      transitions, and tag interactions.
   5. Audit of All Unit Tests: A phased approach to migrate all existing tests in app/src/test/ to ensure no regressions and consistent assertion styles (e.g., switching to org.junit.jupiter.api.Assertions).


  Constraints: Identify any technical risks before proposing the timeline. Provide the plan only; do not implement any changes or modify files.